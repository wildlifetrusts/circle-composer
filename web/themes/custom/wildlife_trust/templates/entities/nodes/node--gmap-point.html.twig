{% if latitude and longitude %}
  <span class="map-data__item" data-lat="{{ latitude }}" data-lng="{{ longitude }}" data-type="{{ type }}" data-uuid="{{ uuid }}" data-title="{{ title }}" />
{%  endif %}
